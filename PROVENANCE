This repository is a projection of a private repository, not the place work happens.

source-commit: 2655b57fcc8ce0390e87ac4df3432937c0fe584f
generated-by:  tools/publish-verifier.sh
files-copied:  62

What that means, and what it does not:

  * The code here is byte-identical to the 5 published crates at that commit. The private
    repository's CI fails if this projection is out of date, so it cannot silently fall behind.
  * You cannot check that claim from here alone — you would need the private repository. What you
    CAN do is the thing that matters: build these crates and re-verify the published log
    yourself. The verification does not trust this repository, or us. It checks signatures.
  * If the two ever disagree, the log is what settles it. Every checkpoint is signed by the
    issuer, and any two that cannot both belong to one honest history are evidence against it
    that anyone can check.
